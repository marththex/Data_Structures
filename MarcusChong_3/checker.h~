#include <iostream>
#include <string>
#include<fstream>
#include "genstack.h"

using namespace std;

class Checker
{
	public:
	
	int count, count1, count2; // count # of  ( or ) / { or }  /  [ or ]
	string inFile;
	string line;
	string textName, textLocation;
	ifstream readFile;
	
	
	Checker();
	~Checker();
	bool readInfile(string textfile);
	int execute();
	
};