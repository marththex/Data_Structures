#include <iostream>
#include <NaiveList.h>
//template <typename E>

class GenQueue
{
	public:
	GenQueue(); 
	GenQueue(int maxSize);
	~GenQueue();
	void insert(char data);
	char remove();
	char peek();
	int isFull();
	int isEmpty();
	int head;
	int tail;
	int mSize;
	int numElements;
	int getSize();
	char *myQueue;
};
