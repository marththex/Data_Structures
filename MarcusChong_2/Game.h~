// assignment 2 - Game.h
#include <iostream>
#include <string>
#include <fstream>
#include <limits>
#include <math.h>
#include <stdlib.h>
#include <cstdlib>

using namespace std;

class ClassicMode{
	public:
		
		ClassicMode();
		~ClassicMode();	

		int x, y, xLine, yLine;
		char ** grid, ** newGrid;
/*
		int randClassic();
		int fileClassic();
		double rand_0_1();

		char randChar(double a);
		int classic();
		int newCell();
		void readInfile(string textfile);
		bool isPopulated(int x, int y);
		
		char choice, c;
		
		double density;
		string inFile, line;
		ifstream readFile;*/

};
